{
  "name": "@reproflow/recorder",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc --noEmit",
    "test": "vitest run src",
    "test:e2e": "vitest run e2e --testTimeout=30000 --hookTimeout=30000"
  },
  "dependencies": {
    "@reproflow/event-schema": "workspace:*",
    "@reproflow/demo-shop": "workspace:*",
    "playwright": "1.63.0"
  }
}
